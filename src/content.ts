// Content script for μDeep Blocker extension
console.log('μDeep Blocker content script loaded');

// Simple content script for initial build
document.addEventListener('DOMContentLoaded', () => {
  console.log('μDeep Blocker content script initialized');
});
